@using MathGamesApp.Core.Models.Problem
@model IEnumerable<DifficultyLevelsViewModel>

@{
    ViewData["Title"] = "Difficulty Level";
}

<div class="row">
    @foreach(var levels in Model)
    {
       <a asp-controller="Problem" asp-action="GenerateProblem" asp-route-difficultyLevelId="@levels.Id">
                        <input type="submit" value="@levels.Name"/>
       </a>

        //<h5 class="levels-card-name">@levels.Name</h5>
    }
</div>